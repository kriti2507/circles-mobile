# Circles Mobile

A social app for forming small friend groups based on shared interests and location. Built with React Native and Expo.

## Getting Started

```bash
# Install dependencies
npm install

# Run the app
npm start
```

Then scan the QR code with the Expo Go app on your phone, or press `i` for iOS simulator / `a` for Android emulator.
